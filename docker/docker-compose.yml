version: '3.8'

services:
  node-monitor:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - MONGODB_URI=mongodb://mongodb:27017
    depends_on:
      - mongodb

  mongodb:
    image: mongo:4.4
    volumes:
      - mongodb_data:/data/db